const utilsHelper = {

    getDefaultLocationMap: function (value) {
        const initialMap = {
            latitude: 0,
            longitude: 0,
            latitudeDelta: 0,
            longitudeDelta: 0
        }

        return initialMap;
    }
}

export default utilsHelper;